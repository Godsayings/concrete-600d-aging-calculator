# -*- coding: utf-8 -*-
"""600℃·d 结构实体检验温度记录 —— 龄期与累计温度自动计算。

算法（口径经实际工程台账与检验资料双向验证）：
  龄期(d)        = 自起算日（浇注日计为第 1 天）起算的自然日差 + 1
  累计温度(℃·d)  = 逐日累加 MAX(日平均温度, 0)
                   即「T > 0 时累加 T；T <= 0 时按 0 计」，累计值只增不减

设计要点：
  1. 台账每个工作表有左右两个数据块（左：日期/平均温度/累计温度/龄期；
     右同构），每块最多 22 行（默认第 6~27 行）。
  2. 累计温度与龄期写为**跨数据块、跨工作表的连续公式链**——
     按数据块重新起算正是实际台账累计值错断的根源。
  3. 公式单元格的缓存值会被 openpyxl 清空，因此由 xmlfix 直接改写
     sheet XML 注入 <v>，使打开即显示数字。
"""

__version__ = '1.0.0'
